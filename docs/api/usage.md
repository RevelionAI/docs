# Credit Usage

## Get Balance

`GET /v1/usage`

Requires `read` scope. Returns credit balance summary for your organization.

## Usage History

`GET /v1/usage/history?limit=50&offset=0`

Requires `read` scope. Returns detailed credit usage per scan/agent.
